# Diagrams

All architecture diagrams in this repo are written as **inline Mermaid** code
blocks directly in the relevant Markdown files (the top-level `README.md` and
each file under `docs/`). GitHub renders Mermaid natively, so they display
without any build step or image assets.

This keeps diagrams version-controlled as text and editable alongside the prose
they describe.
